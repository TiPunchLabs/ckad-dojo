# Implementation Plan: CKAD Simulation 3 Exam

**Branch**: `007-ckad-simulation3` | **Date**: 2025-12-08 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/007-ckad-simulation3/spec.md`

## Summary

Create a third CKAD exam simulation (ckad-simulation3) with Greek mythology theme containing 20 questions covering all CKAD domains. The exam follows the established multi-exam architecture with exam.conf, questions.md, solutions.md, scoring-functions.sh, manifests, and templates.

## Technical Context

**Language/Version**: Bash 4.0+ (scripts), Markdown (content)
**Primary Dependencies**: kubectl, helm, docker
**Storage**: File-based (YAML manifests, markdown files)
**Testing**: Manual testing via scoring scripts, unit tests in tests/
**Target Platform**: Linux with Kubernetes cluster (kubeadm)
**Project Type**: Exam content package following existing architecture
**Performance Goals**: Setup completes in < 2 minutes
**Constraints**: Must follow existing exam structure, no new dependencies
**Scale/Scope**: 20 questions, 105 points, 11 namespaces

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Script-First Automation | PASS | Follows existing setup/score/cleanup pattern |
| II. Kubernetes-Native Tooling | PASS | Uses kubectl, helm, docker only |
| III. Automated Scoring | PASS | scoring-functions.sh implements score_q1-q20 |
| IV. Exam Fidelity | PASS | Questions follow CKAD exam format |
| V. Idempotent Operations | PASS | Inherits from existing scripts |
| VI. Modern UI | PASS | Uses existing web interface |

**Gate Status**: PASSED - No violations

## Project Structure

### Documentation (this feature)

```text
specs/007-ckad-simulation3/
├── spec.md              # Feature specification
├── plan.md              # This file
└── tasks.md             # Task list (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
exams/ckad-simulation3/
├── exam.conf                   # Exam configuration
├── questions.md                # 20 questions in markdown
├── solutions.md                # Solutions for review
├── scoring-functions.sh        # Scoring functions score_q1() - score_q20()
├── manifests/
│   └── setup/                  # Pre-existing resources
│       ├── namespaces.yaml     # 11 namespaces (mythology theme)
│       ├── q08-deployment.yaml # Deployment for rollback question
│       ├── q15-god-pods.yaml   # Pods with labels for Q15
│       ├── q16-deployment.yaml # Deployment for scaling question
│       ├── q18-shadow-pod.yaml # Pod for logs question
│       └── q19-lightning-pod.yaml # Pod for annotations question
└── templates/
    └── q20-image/              # Container build files
        ├── Dockerfile
        └── app.sh
```

**Structure Decision**: Follows existing exam structure from ckad-simulation1 and ckad-simulation2

## Complexity Tracking

No constitution violations - no complexity justification needed.

## Implementation Notes

### Namespace Theme

Greek mythology namespaces: olympus, athena, apollo, artemis, hermes, poseidon, hades, zeus, hera, ares, titan

### Helm Configuration

- Helm namespace: olympus
- Releases: olympus-web-v1, olympus-web-v2, olympus-app (+ broken-release for Q4.4)

### Post-Setup Configuration

- Q8: Deployment `battle-app` in `ares` needs a broken revision created post-setup for rollback exercise

### Registry

Local registry at localhost:5000 for Q20 container build question
